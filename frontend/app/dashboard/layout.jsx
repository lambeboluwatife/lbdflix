export const metadata = {
  title: "LBDflix - User Dashboard",
  description: "LBDflix User dashboard.",
};

const DashboardLayout = ({ children }) => {
  return <div>{children}</div>;
};

export default DashboardLayout;
