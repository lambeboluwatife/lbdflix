export const metadata = {
  title: "LBDflix - Sign Up",
  description: "Sign up to LBDflix.",
};

const SignUpLayout = ({ children }) => {
  return <div>{children}</div>;
};

export default SignUpLayout;
